class Solution(object):
    def isSubsequence(self, s, t):
        ind = 0
        if s == "":
            return True
            
        for i in range(len(t)):

            if t[i] == s[ind]:
                ind += 1
            
            if ind == len(s):
                return True
            
        return False 