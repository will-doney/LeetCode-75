class Solution(object):
    def reverseWords(self, s):
        splits = s.split(" ")
        reverse = []

        for i in splits:
            reverse.insert(0, i)
        
        revString = " ".join(reverse)

        return revString